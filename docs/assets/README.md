# Documentation Assets

This folder stores static assets referenced by documentation.

Recommended structure:

- `screenshots/` for UI captures
- `diagrams/` for exported diagram images when needed

Editable Mermaid sources remain in `docs/diagrams/`.